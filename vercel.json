{
  "version": 2,
  "builds": [
    {
      "src": "/",
      "use": "@vercel/node",
      "config": {
        "commands": [
          "prisma generate",
          "npm run build"
        ]
      }
    }
  ]
}